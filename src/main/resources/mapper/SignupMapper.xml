<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koyoi.main.mapper.SignupMapper">

    <!-- 아이디 중복 체크 -->
    <select id="countUserId" resultType="int">
        SELECT COUNT(*) FROM MAIN_USER WHERE user_id = #{userId}
    </select>

    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="com.koyoi.main.dto.UserDTO">
        INSERT INTO MAIN_USER (
            user_id, user_password, user_name, user_nickname, user_email, user_type, created_at
        ) VALUES (
                     #{user_id},
                     #{user_password},
                     #{user_name},
                     #{user_nickname},
                     #{user_email},
                     2,  -- 기본 사용자 타입 2 (유저)
                     SYSDATE
                 )
    </insert>

</mapper>
